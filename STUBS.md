# Stubs

Any intentional v1 stub (per PRD §21) must be listed here with: what it is, where it lives, and why it's stubbed. Stubs must throw `NotImplementedError` at runtime — never silently no-op.

Currently: **none**.
