/** @file apps/mobile/app/(host)/chats.tsx. */
import { BookingChatsList } from '../../src/features/chat/BookingChatsList';

export default function HostChats() {
  return <BookingChatsList role="host" />;
}
