/** @file apps/mobile/app/(driver)/chat/[bookingId].tsx. */
import { BookingChatThread } from '../../../src/features/chat/BookingChatThread';

export default function ChatThread() {
  return <BookingChatThread role="driver" />;
}
