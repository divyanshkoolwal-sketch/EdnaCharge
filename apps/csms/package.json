{
  "name": "@edna/csms",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --ignore='**/node_modules/**' src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint yet)\"",
    "test": "echo \"(no tests yet)\""
  },
  "dependencies": {
    "@edna/config": "workspace:*",
    "@sentry/node": "^8.30.0",
    "fastify": "^4.28.1",
    "ocpp-rpc": "^2.2.1",
    "pino": "^9.4.0",
    "pino-pretty": "^11.2.2"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
