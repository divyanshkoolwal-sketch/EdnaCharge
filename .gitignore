node_modules
dist
.turbo
.next
.expo
.expo-shared
ios/Pods
android/.gradle
.DS_Store
*.log
.env
.env.*
!.env.example
coverage
*.tsbuildinfo
supabase/.temp
supabase/.branches
dump.rdb

# Native projects are NOT committed — EAS (and local `expo prebuild`) regenerate
# them from app config + config plugins. Keeping them out of git is what prevents
# the SDK-version drift that made the old committed ios/ folder go stale.
apps/mobile/ios/
apps/mobile/android/
